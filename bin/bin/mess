#!/usr/bin/env bash

[[ "$@" == "" ]] && exit 1

LIST="/bin/ls -ANgoh --group-directories-first --color=always"
MESS="${PAGER:=/bin/less}"

if [[ -L "$@" && -d "$@" ]]; then
    ${LIST} "$(readlink $@)" | "${MESS}"
elif [[ -d "$@" ]]; then
    ${LIST} "$@" | "${MESS}"
else
    "${MESS}" "$@"
fi
