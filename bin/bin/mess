#!/usr/bin/env bash

[[ "$@" == "" ]] && exit 1
"${PAGER:="/bin/less"}"
PREVIEW="/bin/ls -1Ga --color=always"

if [[ -L "$@" && -d "$@" ]]; then
    ${PREVIEW} "$(readlink $@)" | "${PAGER}"
elif [[ -d "$@" ]]; then
    ${PREVIEW} "$@" | "${PAGER}"
else
    "${PAGER}" "$@"
fi
