#!/usr/bin/env bash


### Variables definition
########################

FILE="$*"
[[ -x "$(command -v nano)" ]] && NAMES="Nano\n"$NAMES
[[ -x "$(command -v vim)" ]] && NAMES="Vim\n"$NAMES
[[ -x "$(command -v kak)" ]] && NAMES="Kakoune\n"$NAMES
[[ -x "$(command -v nvim)" ]] && NAMES="Neovim\n"$NAMES
[[ -x "$(command -v vim)" ]] && NAMES="Evilvim\n"$NAMES


### Functions definition
########################

# check a function with something like this:
# [[ "$(type -t _vim)" == function ]] && _vim $FILE
if [ -f ~/.bash_functions ]; then
    . ~/.bash_functions
fi


### Greppers
############

echo "$*" | grep "\.ar\." && {
    _xhide xterm -e "${EDITOR:=vi}" "$*" &
    exit
}

echo "$1" | grep "\.sent$" && {
    _xhide sent "$1" &
    exit
}


### MIME-type opener (inode/directory not necessary)
####################################################

# check the relative mime-type here:
# https://www.iana.org/assignments/media-types/media-types.xhtml
case $(file --mime-type "${FILE}" -bL) in
    audio/*)
        clear &&
        _mpv "${FILE}"
        ;;
    video/*)
        _xshow "mpv --force-window --no-terminal --msg-level=all=no '${FILE}'"
        ;;
    application/pdf | application/postscript | application/djvu | image/vnd.djvu | application/epub+zip)
        _xshow "zathura '${FILE}'"
        ;;
     application/x-tar | application/zip | application/gzip)
        [[ "${FILE##*.}" == "xopp" ]] && _xshow "xournalpp '${FILE}'" || _xshow "xarchiver '${FILE}'"
        ;;
    image/*)
        _xshow "esxiv '${FILE}'"
        ;;
    *)
        if [[ ! -n $NAMES  || ! -f "/bin/fzf" ]]; then
            "${EDITOR:=vi}" "${FILE}"
            exit 0
        fi
        case $(printf "$NAMES" | fzf --prompt="Edit ${FILE##*/} with: " --height 100% --reverse --info=hidden --header-first) in
            Evilvim)
                _vim "${FILE}"
                ;;
            Neovim)
                nvim "${FILE}"
                ;;
            Kakoune)
                kak "${FILE}"
                ;;
            Vim)
                _vimnoplugin "${FILE}"
                ;;
            Nano)
                nano "${FILE}"
                ;;
            *)
                exit 1
        esac
esac
