#!/bin/sh

# An example of a list dialogue using Zenity.
# Other types of Zenity dialogues are:
#   --calendar
#   --color-selection
#   --file-selection
#   --forms
#   --notification
#   --password
#   --progress
#   --scale
#   --entry
#   --text-info
#
# For more info, visit the Zenity manual:
# https://help.gnome.org/users/zenity/stable/


EXITI3=$(zenity --list \
            --title='Exit I3' \
            --column='Mode' --column='Command' \
                Logout 'i3-msg exit' \
                Reboot 'i3-msg exec reboot' \
                Poweroff 'i3-msg exec powersoff')

case "$EXITI3" in
    "Logout")
        i3-msg exit
        ;;
    "Reboot")
        i3-msg exec reboot
        ;;
    "Poweroff")
        i3-msg exec poweroff
        ;;
esac
